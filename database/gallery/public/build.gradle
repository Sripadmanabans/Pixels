plugins {
  id "com.adjectivemonk2.jvm.lib"
}

archivesBaseName = "database-gallery-public"

dependencies {
  api projects.model.gallery.public

  api libs.kotlin.coroutines.core
}

kotlin {
  sourceSets.all {
    languageSettings {
      optIn("kotlin.time.ExperimentalTime")
    }
  }
}
