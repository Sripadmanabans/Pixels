plugins {
  id "com.adjectivemonk2.jvm.lib"
}

archivesBaseName = "local-session-public"

dependencies {
  api libs.kotlin.coroutines.core
  api libs.kotlin.time
}

kotlin {
  sourceSets.configureEach {
    languageSettings {
      optIn("kotlin.time.ExperimentalTime")
    }
  }
}
