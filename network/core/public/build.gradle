plugins {
  alias libs.plugins.kotlin.jvm
}

archivesBaseName = "network-core-public"

dependencies {
  api libs.retrofit.client

  implementation libs.dagger.runtime
  implementation libs.kotlin.coroutines.core
}

kotlin {
  explicitApi()
}
