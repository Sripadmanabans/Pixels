style:
  # This is picking up @Preview annotated methods of compose as unused
  UnusedPrivateMember:
    active: false
naming:
  # This is picking up @Compose as having the wrong function name
  FunctionNaming:
    active: false