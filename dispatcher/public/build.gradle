plugins {
  alias libs.plugins.kotlin.jvm
}

archivesBaseName = "dispatcher-public"

dependencies {
  api libs.kotlin.coroutines.core

  implementation libs.dagger.runtime
}

kotlin {
  explicitApi()
}
